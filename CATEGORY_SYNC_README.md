# 分类同步功能实现说明

## 功能概述

实现了分类数据的本地缓存和服务器同步功能，确保在进入transaction页面时能够快速加载分类数据，同时支持离线使用。

## 主要更改

### 1. API调用方式更改
- **API**: `GET /api/user/category/list?income_expense=income`
- **参数**: 通过URL查询参数传递 `income_expense` 值

### 2. 本地存储功能
- 添加了本地存储机制，使用 `uni.setStorageSync` 和 `uni.getStorageSync`
- 存储键名: `local_categories`
- 支持离线时使用本地缓存数据

### 3. 智能数据获取策略
- **优先使用本地缓存**: 如果有本地数据，立即返回，提升用户体验
- **后台同步**: 本地无数据时从服务器获取并保存到本地
- **网络失败回退**: 网络请求失败时使用本地缓存数据

### 4. 新增功能函数

#### `getCategoryList(data)`
- 智能获取分类列表
- 优先使用本地缓存
- 自动同步服务器数据到本地

#### `refreshCategoryList(data)`
- 强制从服务器刷新数据
- 更新本地存储
- 用于手动刷新场景

#### `clearLocalCategories()`
- 清除本地分类缓存
- 用于调试或重置场景

### 5. UI改进
- 添加了刷新按钮 (🔄)
- 支持手动刷新分类数据
- 显示加载状态和错误提示

## 使用流程

1. **首次进入页面**: 
   - 本地无数据 → 从服务器获取 → 保存到本地 → 显示数据

2. **再次进入页面**:
   - 本地有数据 → 立即显示本地数据 → 后台同步服务器数据

3. **切换收入/支出类型**:
   - 根据类型过滤本地数据
   - 如果本地无该类型数据，从服务器获取

4. **手动刷新**:
   - 点击刷新按钮 → 强制从服务器获取最新数据 → 更新本地存储

## 错误处理

- 网络请求失败时自动使用本地缓存
- 本地缓存也失败时显示错误提示
- 详细的错误日志记录

## 性能优化

- 减少不必要的网络请求
- 快速响应本地数据
- 后台异步同步
- 避免重复数据存储

## 测试建议

1. 测试首次加载（无本地数据）
2. 测试再次加载（有本地数据）
3. 测试网络断开时的离线使用
4. 测试手动刷新功能
5. 测试收入/支出类型切换 