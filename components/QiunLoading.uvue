<template>
  <view class="loading-container">
    <view v-if="loadingType == 1" class="loading loading1">
      <view class="loading-dot"></view>
      <view class="loading-dot"></view>
      <view class="loading-dot"></view>
    </view>
    <view v-else-if="loadingType == 2" class="loading loading2">
      <view class="loading-circle"></view>
    </view>
    <view v-else-if="loadingType == 3" class="loading loading3">
      <view class="loading-bar"></view>
      <view class="loading-bar"></view>
      <view class="loading-bar"></view>
    </view>
    <view v-else-if="loadingType == 4" class="loading loading4">
      <view class="loading-square"></view>
    </view>
    <view v-else-if="loadingType == 5" class="loading loading5">
      <view class="loading-pulse"></view>
    </view>
  </view>
</template>

<script>
export default {
  name: 'QiunLoading',
  props: {
    loadingType: {
      type: Number,
      default: 2
    }
  }
};
</script>

<style>
.loading-container {
  width: 100%;
  height: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
}

.loading {
  display: flex;
  justify-content: center;
  align-items: center;
}

/* Loading Type 1 - Dots */
.loading1 {
  gap: 8px;
}

.loading1 .loading-dot {
  width: 12px;
  height: 12px;
  border-radius: 50%;
  background-color: #007AFF;
  animation: loading1 1.4s ease-in-out infinite both;
}

.loading1 .loading-dot:nth-child(1) { animation-delay: -0.32s; }
.loading1 .loading-dot:nth-child(2) { animation-delay: -0.16s; }

@keyframes loading1 {
  0%, 80%, 100% {
    transform: scale(0);
    opacity: 0.5;
  }
  40% {
    transform: scale(1);
    opacity: 1;
  }
}

/* Loading Type 2 - Circle */
.loading2 .loading-circle {
  width: 40px;
  height: 40px;
  border: 4px solid #f3f3f3;
  border-top: 4px solid #007AFF;
  border-radius: 50%;
  animation: loading2 1s linear infinite;
}

@keyframes loading2 {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

/* Loading Type 3 - Bars */
.loading3 {
  gap: 4px;
}

.loading3 .loading-bar {
  width: 4px;
  height: 20px;
  background-color: #007AFF;
  animation: loading3 1.2s ease-in-out infinite;
}

.loading3 .loading-bar:nth-child(1) { animation-delay: -0.4s; }
.loading3 .loading-bar:nth-child(2) { animation-delay: -0.2s; }

@keyframes loading3 {
  0%, 40%, 100% {
    transform: scaleY(0.4);
  }
  20% {
    transform: scaleY(1);
  }
}

/* Loading Type 4 - Square */
.loading4 .loading-square {
  width: 30px;
  height: 30px;
  background-color: #007AFF;
  animation: loading4 1.2s ease-in-out infinite;
}

@keyframes loading4 {
  0% {
    transform: rotate(0deg);
  }
  50% {
    transform: rotate(180deg);
  }
  100% {
    transform: rotate(360deg);
  }
}

/* Loading Type 5 - Pulse */
.loading5 .loading-pulse {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background-color: #007AFF;
  animation: loading5 1.2s ease-in-out infinite;
}

@keyframes loading5 {
  0% {
    transform: scale(0);
    opacity: 1;
  }
  100% {
    transform: scale(1);
    opacity: 0;
  }
}
</style> 